# c401 / c581 Spine 上躯干语义审计

日期：2026-09-21  
状态：`READY_OFFLINE`

## 范围

本审计只验证 Character Card v2 的上躯干/core axis 语义选择，不改 Calendar、账号、Signin、Voice、Daily/CDK、QQ/NapCat 或部署路径。bundle 从用户自有 `serv` 服务器只读取得，原始 `.skel/.atlas` 不提交仓库。

## 真实 bundle 与运行时

| 资源 | Skeleton 版本 | 匹配 Core | Skeleton SHA-256 | Atlas SHA-256 |
| --- | --- | --- | --- | --- |
| c401 | 4.0.47 | Spine Core 4.0.31 | `3616119c7aaa9b5a1be350b45255e3b808d5f435ff4f2451d6ccf7f13576e844` | `b1675a545b65dc2417ca17bad24eb02c95b1f97a497da67ba14f7d4729aa61d0` |
| c581 | 4.1.20 | Spine Core 4.1.56 | `d4d95833deb15a2656ade2b9b029c5e81968f60900ef05100cd493190cc7e504` | `7f3df9b03eed0a660c0ba9ccd6dc201e19284901108ced9b5e8020f7319c65c3` |

采样使用 `idle`，时间点为 `t=0` 与 `t=0.5`。匹配 Core 仅执行离线 sidecar 发现；产品运行时不加载提取脚本，也不下载或请求这些服务器文件。

## 选择结果

- c401：真实结构没有可靠的 `chest`/`torso` 骨骼名。唯一可接受的主体 attachment 是 `body_total/body_total`，挂在 `move3`（父级 `move2`）；其余 `op*`、头发、武器和外套候选不进入 generic heuristic。通过 `spine_bone_overrides.json` 的结构化 verified attachment 覆盖，idle@0 点为 `[507.172174, 385.607414]`，置信度 `0.98`。
- c581：真实结构存在同父级 `pelvis7` 的 `chest_l` / `chest_r`，通过 `spine_bone_semantics.json` 的 verified bone pair 选择，idle@0 中点为 `[533.207679, 306.425512]`，置信度 `0.96`。

### 多帧稳定性

| 资源 | eye @ 0 | torso @ 0 | eye @ 0.5 | torso @ 0.5 |
| --- | --- | --- | --- | --- |
| c401 | `[508.486262, 223.514768]` | `[507.172174, 385.607414]` | `[507.849337, 223.782285]` | `[506.806128, 384.646980]` |
| c581 | `[498.871888, 164.852725]` | `[533.207679, 306.425512]` | `[499.453737, 164.320685]` | `[535.263117, 298.218901]` |

点位随动画变化，不能固化成屏幕坐标或一次性平均值；运行时 metadata 只保存已验证的最终点，不保存 discovery sidecar 的全候选列表。

## 证据边界

- 本文和同名 JSON 是真实 bundle 的离线结构证据，不是账号数据、QQ 送达、生产部署或资源授权证明。
- 未提交原始 Spine bundle；未声称 c401/c581 已进入 `assets/spine_manifest.json` 的可渲染产品清单。
- 如果未来要把新的 bundle 纳入正式渲染清单，必须重新完成来源、许可、runtime、纹理完整性和 PNG 视觉验收。
